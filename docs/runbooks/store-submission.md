# Store submission

Submit only a protected, reviewed commit whose automated and real-device gates are complete. Confirm binary identity, metadata, privacy declarations, account-deletion access, review navigation, and organization ownership of signing and store accounts. Never use a client redirect as proof of server-side transaction state.
