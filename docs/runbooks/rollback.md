# Rollback

When a released shell is unsafe or unusable, pause rollout, disable affected optional capabilities through permitted configuration, restore compatible web behavior, and ship a previously approved or corrected binary through the stores. Configuration must never add trust or weaken TLS and bridge checks.
