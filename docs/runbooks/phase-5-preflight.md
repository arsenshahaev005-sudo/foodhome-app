# Release preflight

Before creating a release candidate, require a clean protected commit, passing unsigned CI, reviewed dependency changes, current privacy and store disclosures, organization-owned signing access, and a completed real-device matrix. A static repository check alone does not establish release readiness.
